<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ══════ Sidebar-specific colors (not in generic Colors.xaml) ══════ -->
    <SolidColorBrush x:Key="SidebarBaseBrush"        Color="#1C2536"/>
    <SolidColorBrush x:Key="SidebarCardBrush"        Color="#2D3748"/>
    <SolidColorBrush x:Key="SidebarCardDarkBrush"    Color="#374151"/>
    <SolidColorBrush x:Key="SidebarHoverBrush"       Color="#2D3748"/>
    <SolidColorBrush x:Key="SidebarActiveTextBrush"  Color="#1F2937"/>
    <SolidColorBrush x:Key="SidebarHoverTextBrush"   Color="#E5E7EB"/>

    <!-- ══════ Sidebar Button ══════ -->
    <Style x:Key="SidebarButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="12,10"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="FontSize" Value="{StaticResource FontSizeLg}"/>
        <Setter Property="FontWeight" Value="Normal"/>
        <Setter Property="Foreground" Value="{StaticResource SidebarTextBrush}"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Margin" Value="8,1"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="ButtonBorder"
                            Background="{TemplateBinding Background}"
                            CornerRadius="{StaticResource RadiusMd}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ButtonBorder" Property="Background"
                                    Value="{StaticResource SidebarHoverBrush}"/>
                            <Setter Property="Foreground"
                                    Value="{StaticResource SidebarHoverTextBrush}"/>
                        </Trigger>
                        <Trigger Property="Tag" Value="Selected">
                            <Setter TargetName="ButtonBorder" Property="Background"
                                    Value="{StaticResource AccentBrush}"/>
                            <Setter Property="Foreground"
                                    Value="{StaticResource SidebarActiveTextBrush}"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ══════ User Card ══════ -->
    <Style x:Key="SidebarUserCardStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource SidebarCardBrush}"/>
        <Setter Property="Padding" Value="12,10"/>
        <Setter Property="Margin" Value="10,10,10,12"/>
        <Setter Property="CornerRadius" Value="8"/>
    </Style>

    <!-- ══════ Active Company Card ══════ -->
    <Style x:Key="SidebarActiveCompanyCardStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource SidebarCardBrush}"/>
        <Setter Property="CornerRadius" Value="{StaticResource RadiusXl}"/>
        <Setter Property="Padding" Value="12"/>
    </Style>

    <!-- ══════ Avatar Circle ══════ -->
    <Style x:Key="SidebarAvatarCircleStyle" TargetType="Border">
        <Setter Property="Width" Value="32"/>
        <Setter Property="Height" Value="32"/>
        <Setter Property="CornerRadius" Value="16"/>
        <Setter Property="Background" Value="{StaticResource SidebarCardDarkBrush}"/>
    </Style>

    <!-- ══════ Logo Box ══════ -->
    <Style x:Key="SidebarLogoBoxStyle" TargetType="Border">
        <Setter Property="Width" Value="40"/>
        <Setter Property="Height" Value="40"/>
        <Setter Property="CornerRadius" Value="{StaticResource RadiusXl}"/>
        <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
    </Style>

    <!-- ══════ Status Badge ══════ -->
    <Style x:Key="SidebarStatusBadgeStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource SuccessBrush}"/>
        <Setter Property="CornerRadius" Value="{StaticResource RadiusSm}"/>
        <Setter Property="Padding" Value="6,2"/>
    </Style>

</ResourceDictionary>