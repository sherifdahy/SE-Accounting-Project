<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ══════ State Badge (Border) ══════ -->
    <Style x:Key="StateBadge" TargetType="Border">
        <Setter Property="CornerRadius" Value="12"/>
        <Setter Property="Padding" Value="8,3"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

    <!-- ══════ State Badge Text ══════ -->
    <Style x:Key="StateBadgeText" TargetType="TextBlock">
        <Setter Property="FontSize" Value="{StaticResource FontSizeXs}"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

    <!-- ══════ State Arrow ══════ -->
    <Style x:Key="StateArrowText" TargetType="TextBlock">
        <Setter Property="Text" Value="←"/>
        <Setter Property="Margin" Value="6,0"/>
        <Setter Property="FontSize" Value="{StaticResource FontSizeSm}"/>
        <Setter Property="Foreground" Value="{StaticResource TextPlaceholderBrush}"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

    <!-- ══════ History Timeline Icon ══════ -->
    <Style x:Key="HistoryTimelineIcon" TargetType="Border">
        <Setter Property="Width" Value="36"/>
        <Setter Property="Height" Value="36"/>
        <Setter Property="CornerRadius" Value="18"/>
        <Setter Property="Margin" Value="0,0,12,0"/>
        <Setter Property="VerticalAlignment" Value="Top"/>
    </Style>

    <!-- ══════ History Meta ══════ -->
    <Style x:Key="HistoryMetaText" TargetType="TextBlock">
        <Setter Property="FontSize" Value="{StaticResource FontSizeXs}"/>
        <Setter Property="Foreground" Value="{StaticResource TextPlaceholderBrush}"/>
    </Style>

    <Style x:Key="HistoryUserText" TargetType="TextBlock">
        <Setter Property="FontSize" Value="{StaticResource FontSizeXs}"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
    </Style>

    <Style x:Key="HistoryNoteText" TargetType="TextBlock">
        <Setter Property="FontSize" Value="{StaticResource FontSizeXs}"/>
        <Setter Property="Foreground" Value="{StaticResource TextMutedBrush}"/>
        <Setter Property="TextWrapping" Value="Wrap"/>
        <Setter Property="MaxWidth" Value="250"/>
        <Setter Property="HorizontalAlignment" Value="Right"/>
        <Setter Property="Margin" Value="0,3,0,0"/>
    </Style>

    <Style x:Key="HistoryDateText" TargetType="TextBlock">
        <Setter Property="FontSize" Value="{StaticResource FontSizeXs}"/>
        <Setter Property="Foreground" Value="{StaticResource TextPlaceholderBrush}"/>
        <Setter Property="FlowDirection" Value="LeftToRight"/>
        <Setter Property="HorizontalAlignment" Value="Right"/>
        <Setter Property="Margin" Value="0,4,0,0"/>
    </Style>

    <!-- ══════ Info Date Value ══════ -->
    <Style x:Key="InfoDateValue" TargetType="TextBlock"
           BasedOn="{StaticResource InfoValue}">
        <Setter Property="FlowDirection" Value="LeftToRight"/>
        <Setter Property="HorizontalAlignment" Value="Right"/>
    </Style>

</ResourceDictionary>